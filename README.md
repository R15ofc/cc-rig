# RIG

Paused / archived for now.

DockOS is now standalone and should be installed directly:

```lua
wget run https://raw.githubusercontent.com/R15ofc/cc-dock/main/dock-installer.lua
dock
```

This repository remains online only so old raw links and experiments do not break.
