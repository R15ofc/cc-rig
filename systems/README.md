# Systems

This directory contains the planned product systems that sit on top of the existing `rig-hub/` and `cc/` runtime.

- `rig/` - developer platform and package APIs.
- `dock/` - OS shell and app environment.
- `luma/` - browser, domains, search, and site builder.
- `shared/` - accounts, package trust, permissions, and cross-system contracts.

