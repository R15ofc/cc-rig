# RIG API

Planned API areas:

- package registry;
- package upload;
- package trust state;
- package removal/blocking;
- account/session integration;
- app store metadata;
- Luma domain/site metadata.

The existing FastAPI implementation lives in `rig-hub/`.

