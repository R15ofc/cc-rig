# RIG SDK

The SDK should provide simple APIs for applications:

- package metadata access;
- account/session access;
- app permissions;
- notifications;
- Luma site/app integration;
- Dock app integration.

SDKs should be explicit and minimal. Apps should not need to know hub internals.

