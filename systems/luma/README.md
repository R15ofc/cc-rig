# Luma

Luma is the in-game internet layer.

It should provide:

- browser app;
- domain registry;
- search;
- site/app format;
- website builder;
- publishing through RIG packages;
- verified/unverified site labels.

Luma should feel like a real network inside the CC ecosystem, not just a static page viewer.

