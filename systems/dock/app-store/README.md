# Dock App Store

The Dock App Store should consume RIG package registry data.

Required UI states:

- verified package;
- unreviewed package warning;
- warned package warning;
- blocked package install refusal;
- installed package removal;
- update available;
- source/repo link.

