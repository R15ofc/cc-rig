# Permissions

Apps and packages should request permissions explicitly.

Examples:

- `network.http`;
- `files.read`;
- `files.write`;
- `shell.launch`;
- `package.install`;
- `luma.publish`;
- `dock.notifications`;

Dock and RIG clients should show permissions before install or first run.

