# Package Trust

Package trust data is owned by RIG Hub and displayed by Dock, Luma, and RIG CLI.

Trust states:

- `unreviewed`;
- `verified`;
- `warned`;
- `blocked`;
- `removed`.

Clients should refuse blocked packages and warn for unreviewed or warned packages.

